## Normalização Min-Max por Banda

Aplica normalização Min-Max a cada banda das matrizes, escalando os valores para o intervalo [0, 1].

- **Entrada:** Lista de matrizes `(bandas, altura, largura)`
- **Saída:** Lista de matrizes normalizadas (`np.float32`)